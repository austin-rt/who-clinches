# Rename Project: sec-tiebreaker → who-clinches

**Status**: ✅ Completed

> **Note**: This rename has been completed. The project is now named "who-clinches" (see `package.json` and README.md). This document is kept for historical reference only.

## Summary

The project was renamed from `sec-tiebreaker` to `who-clinches` to better reflect the product purpose (determining who clinches playoff spots), improve scalability beyond SEC, and create a cleaner brand identity.

**Key Changes:**
- Repository renamed on GitHub (git history preserved)
- `package.json` name updated
- User-Agent strings updated in `lib/cfb/espn-client.ts`
- Vercel project renamed (URLs changed to `who-clinches-git-*-*.vercel.app`)
- `MONGODB_APP_NAME` environment variable updated
- Documentation URLs updated

**Breaking Changes:**
- Vercel deployment URLs changed (old bookmarks/external links may break)
